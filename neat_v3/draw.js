let population = new Population();

let total_pop = 50,
mutation_rate = 0.005;

function setup(){
    createCanvas(400, 600);
}


let check = false

function draw(){
    background(0);
}

